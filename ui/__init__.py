"""AgentEvac operator console.

A demonstration user interface that wraps the simulator without modifying it.
Nothing under ``agentevac/`` is imported for its side effects except by
``ui.bridge.launcher``, which runs the unmodified simulation script inside a
process it owns.
"""
