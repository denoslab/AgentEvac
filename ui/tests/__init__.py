"""Tests for the operator console.

These are separate from the research test suite under ``tests/``, so
``python -m pytest tests/`` still runs exactly what it ran before the console
existed. Run these with ``python -m pytest ui/tests/``.
"""
