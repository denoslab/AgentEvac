"""Simulation entry point and vehicle spawn configuration.

    main         — main SUMO/TraCI simulation loop (``python -m agentevac.simulation.main``).
    spawn_events — static vehicle spawn schedule (veh_id, edge, depart_time, …).
"""
